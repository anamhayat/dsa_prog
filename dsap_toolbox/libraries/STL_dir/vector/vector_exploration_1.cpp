#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

// template <class T>
// vector<T> bsort(vector<T> &x)
// {
//     for (int i = 0; i < x.size(); i++)
//     {
//         // T key = x.at(i);
//         for (auto j = x.begin(); j != x.end() - i - 1; j++)
//         {
//             if (*j < *(j + 1))
//             {
//                 T temp = *j;
//                 *j = *(j + 1);
//                 *(j + 1) = temp;
//             }
//         }
//     }
// }
int main()
{
    // vector<int> x = {4, 6, 2, 4, 2, 2};
    // bsort(x);
    // for (auto i = x.begin(); i != x.end(); i++)
    //     cout << *i << " ";
    // do
        cout << "Hello";
    // while (1 == 0);
    return 0;
}
